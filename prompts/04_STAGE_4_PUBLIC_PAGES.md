# STAGE 4 — PUBLIC PAGES

Implement all remaining PUBLIC pages only.
Precondition: Stage 3 PASS.

For EACH route:
1. use the Stage 0 mapping to open its matching file in `mockup/`;
2. inspect matching root `assets/`;
3. implement that page according to its own mock-up;
4. report any asset/mapping gap instead of silently inventing a design.

Routes:
- /despre-noi
- /proiecte
- /proiecte/[slug]
- /implica-te
- /transparenta
- /noutati
- /noutati/[slug]
- /contact
- required legal pages based on project scope

Do not implement admin CMS yet.
Transparency may show empty/controlled demo state; no upload yet.
Contact may have complete UI but real submit can remain [DE CONFIGURAT] if no provider exists.

Metadata per public page.
Run route checks, lint, build, git diff --check.

Output:
### Stage 4 result
### Route -> mock-up mapping used
### Route -> assets used
### Missing/ambiguous mappings
### Routes implemented
### Content strategy
### SEO
### Lint/build
### Missing real content
### Recommended commit message

STOP.
